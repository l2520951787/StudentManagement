<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech.StudentManagement.repository.StudentRepository">

  <!-- 受講生情報の全件検索 -->
  <select id="getStudentList" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
  </select>

  <!-- 受講生情報の検索 -->
  <select id="searchStudentById" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students WHERE id = #{id}
  </select>

  <!-- コース情報の全件検索　-->
  <select id="getStudentCourseList" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses
  </select>

  <!-- コース情報の検索　-->
  <select id="searchStudentCourseByStudentId"
    resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses WHERE student_id = #{studentId}
  </select>

  <!-- 受講生情報の登録　-->
  <insert id="registerStudent" parameterType="raisetech.StudentManagement.data.Student"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students(name, ruby, nickname, mail_address, area, age, gender, remark, deleted)
    VALUES(#{name}, #{ruby}, #{nickname}, #{mailAddress}, #{area}, #{age}, #{gender}, #{remark},
    false)
  </insert>

  <!-- コース情報の登録　-->
  <insert id="registerStudentCourse" parameterType="raisetech.StudentManagement.data.StudentCourse"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students_courses(student_id, course, start_date, end_date) VALUES(#{studentId},
    #{course}, #{startDate}, #{endDate})
  </insert>

  <!-- 受講生情報の更新 -->
  <update id="updateStudent" parameterType="raisetech.StudentManagement.data.Student">
    UPDATE students SET name = #{name}, ruby = #{ruby}, nickname = #{nickname},
    mail_address = #{mailAddress}, area = #{area}, age = #{age}, gender = #{gender}, remark =
    #{remark},
    deleted = #{deleted} WHERE id = #{id}
  </update>

  <!-- コース情報の更新 -->
  <update id="updateStudentCourse" parameterType="raisetech.StudentManagement.data.StudentCourse">
    UPDATE students_courses SET course = #{course} WHERE id = #{id}
  </update>
</mapper>